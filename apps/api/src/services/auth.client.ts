// Replaces the old local auth.client.ts — now sourced from the shared package
export { AuthServiceClient, authServiceClient } from "@nexus/auth-client";
export type { VerifyTokenResponse } from "@nexus/auth-client";
